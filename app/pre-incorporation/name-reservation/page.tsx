import { PreIncorporationLayout } from "@/components/pre-incorporation/pre-incorporation-layout"

export const metadata = {
  title: "Name Reservation Review",
  description: "Review and process name reservation applications",
}

export default function NameReservationPage() {
  return (
    <PreIncorporationLayout activeTab="reservation">
      <></>
    </PreIncorporationLayout>
  )
}
